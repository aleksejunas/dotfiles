#-- General ----------------------------------------------------
general {
    lock_cmd = ~/.config/hypr/scripts/lockscreen
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}

# set monitor backlight to minimum, avoid 0 on OLED monitor
listener {
    timeout = 240
    on-timeout = light -O && light -S 5
    on-resume = light -I
}

# turn off keyboard backlight, comment out this section if you dont have a keyboard backlight.
#listener { 
#    timeout = 120
#    on-timeout = brightnessctl -sd rgb:kbd_backlight set 0
#    on-resume = brightnessctl -rd rgb:kbd_backlight
#}

# screen off when timeout has passed (before lock)
listener {
    timeout = 900
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}



# Optional: Manual suspend after very long inactivity (disabled by default)
# listener {
#     timeout = 1800
#     on-timeout = systemctl suspend
# }

# Suspend after 3600 seconds of inactivity (1 hour) if on battery
# listener {
#   timeout = 3600
#   on-timeout = sh -c 'upower -i $(upower -e | grep BAT) | grep -q "state_.*fully-charged" && systemctl suspend'
# }

# Suspend after 7200 seconds of inactivity (2 hours) if on AC
# listener {
#   timeout = 7200
#   on-timeout = sh -c 'upower -i $(upower -e | grep BAT) | grep -q "state:.*fully-charged" && systemctl suspend'
# }



# Auto-suspend with simple swaylock (10 minutes for testing)
listener {
    timeout = 600
    on-timeout = ~/.config/hypr/scripts/simple_suspend
}

