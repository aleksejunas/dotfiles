-- Plugin: supermaven-inc/supermaven-nvim
-- Installed via store.nvim

return {
    "supermaven-inc/supermaven-nvim",
    config = function()
        require(
            "supermaven-nvim"
        ).setup({})
    end
}